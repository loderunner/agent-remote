{
  "name": "@agent-remote/integration-tests",
  "version": "1.0.0",
  "description": "Integration tests for agent-remote tool implementations",
  "license": "Apache-2.0",
  "author": "Charles Francoise",
  "repository": {
    "type": "git",
    "url": "https://github.com/loderunner/agent-remote.git",
    "directory": "packages/integration-tests"
  },
  "keywords": [
    "tests",
    "integration",
    "agent-remote"
  ],
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ssh": "vitest run --testNamePattern ssh",
    "test:watch:ssh": "vitest --testNamePattern ssh",
    "test:docker": "vitest run --testNamePattern docker",
    "test:watch:docker": "vitest --testNamePattern docker"
  },
  "dependencies": {
    "@agent-remote/core": "workspace:*",
    "@agent-remote/docker": "workspace:*",
    "@agent-remote/ssh": "workspace:*",
    "ssh2": "^1.17.0"
  },
  "devDependencies": {
    "@types/ssh2": "^1.15.5",
    "jest-extended": "^4.0.2",
    "rimraf": "^6.0.1",
    "tslib": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
